description: The responses for the given message have been retrieved successfully.
headers:
  $ref: ../../snippets/StandardResponseHeaders.yaml
content:
  application/json:
    schema:
      $ref: ../../schemas/responses/MessageResponses.yaml
