description: |+
  The `Content-Type` of the request is not supported. This endpoint supports `application/json`.
content:
  application/json:
    schema:
      $ref: ../../schemas/responses/errors/UnsupportedMedia.yaml
headers:
  $ref: ../../snippets/StandardResponseHeaders.yaml
