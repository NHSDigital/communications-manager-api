title: GetResponsesSuccess
type: object
additionalProperties: false
required:
  - messageId
  - responses
properties:
  messageId:
    $ref: ../types/KSUID.yaml
  responses:
    type: array
    items:
      $ref: ../components/ResponseItem.yaml
