type: object
title: Unable to get responses
additionalProperties: false
required:
  - error
properties:
  error:
    type: string
    enum:
      - Invalid messageId format
    example: Invalid messageId format
