type: object
title: Service unavailable
additionalProperties: false
properties:
  errors:
    type: array
    minItems: 1
    maxItems: 1
    uniqueItems: true
    items:
      type: object
      properties:
        id:
          $ref: TypeErrorIdentifier.yaml
        code:
          $ref: enums/EnumErrorServiceUnavailable.yaml
        links:
          $ref: LinksError.yaml
        status:
          type: string
          enum:
            - '503'
          example: '503'
        title:
          type: string
          enum:
            - The service is currently unavailable
          example: The service is currently unavailable
        detail:
          type: string
          enum:
            - The service is currently not able to process this request, try again later.
          example: The service is currently not able to process this request, try again later.
