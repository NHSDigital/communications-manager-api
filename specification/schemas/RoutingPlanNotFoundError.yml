type: object
title: No such routing plan
additionalProperties: false
properties:
  errors:
    type: array
    minItems: 1
    maxItems: 1
    uniqueItems: true
    items:
      type: object
      properties:
        id:
          $ref: TypeErrorIdentifier.yml
        code:
          $ref: enums/EnumErrorNoSuchRoutingPlan.yml
        links:
          $ref: LinksError.yml
        status:
          type: string
          enum:
            - '404'
          example: '404'
        title:
          type: string
          enum:
            - No such routing plan
          example: No such routing plan
        detail:
          type: string
          enum:
            - The routing plan specified either does not exist or is not in a usable state.
          example: The routing plan specified either does not exist or is not in a usable state.
        source:
          type: object
          properties:
            pointer:
              type: string
              enum:
                - /data/attributes/routingPlan
              example: /data/attributes/routingPlan
