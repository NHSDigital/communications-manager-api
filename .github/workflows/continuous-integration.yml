name: Create Release
on: push

jobs:
  create_release:
    name: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master'
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0  # This causes all history to be fetched, which is required for calculate-version to function

      - name: Install Python 3.13
        uses: actions/setup-python@v4
        with:
          python-version: 3.13

      - name: Upgrade python pip
        run: python -m pip install --upgrade pip

      - name: Install git
        run: pip install gitpython

      - name: Install semver
        run: pip install semver

      - name: Set SPEC_VERSION env var
        run: echo ::set-env name=SPEC_VERSION::$(python scripts/calculate_version.py)
        env:
          ACTIONS_ALLOW_UNSECURE_COMMANDS: true

      - name: Create release (master only)
        id: create-release
        if: github.ref == 'refs/heads/master'
        uses: NHSDigital/nhs-notify-shared-modules/.github/actions/create-github-release@PLACEHOLDER # 4.0.8 - TODO: replace PLACEHOLDER with actual tag SHA once shared-modules 4.0.8 is released
        continue-on-error: true
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          tag_name: ${{ env.SPEC_VERSION }}
          release_name: ${{ env.SPEC_VERSION }}

